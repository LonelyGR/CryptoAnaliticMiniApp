# 🔧 Проверка и исправление подключения

## ⚡ Быстрая проверка (5 минут)

### Шаг 1: Проверьте .env файл

```bash
cd miniapp/react-app
node diagnose_connection.js
```

Этот скрипт проверит:
- ✅ Существует ли .env файл
- ✅ Правильный ли формат
- ✅ Правильный ли URL

### Шаг 2: Проверьте подключение к серверу

```bash
node test_api_connection.js https://ваш-backend-url.com
```

Замените `https://ваш-backend-url.com` на ваш реальный URL из .env

### Шаг 3: Проверьте консоль браузера

1. Откройте приложение в Telegram
2. Откройте DevTools (через меню Telegram)
3. Найдите в консоли:
   ```
   API Base URL configured: ...
   REACT_APP_API_URL env: ...
   ```

**Если видите:**
- `REACT_APP_API_URL env: undefined` → .env не загружен
- `API Base URL configured: http://localhost:8000` → используется значение по умолчанию

## 🔧 Исправление

### Вариант 1: Автоматическое создание .env

**Windows:**
```bash
cd miniapp/react-app
create_env.bat
```

**Linux/Mac:**
```bash
cd miniapp/react-app
chmod +x create_env.sh
./create_env.sh
```

### Вариант 2: Ручное создание

1. Создайте файл `miniapp/react-app/.env`
2. Добавьте строку:
   ```
   REACT_APP_API_URL=https://ваш-backend-url.com
   ```
3. **ОБЯЗАТЕЛЬНО перезапустите приложение:**
   ```bash
   # Остановите (Ctrl+C)
   npm start
   ```

## ✅ Проверка результата

После исправления в консоли браузера должно быть:
```
API Base URL configured: https://ваш-url.com
REACT_APP_API_URL env: https://ваш-url.com
Checking API health at: https://ваш-url.com
API health check result: true 200
```

Если видите эти сообщения - все работает! 🎉

## ❌ Если все еще не работает

1. **Проверьте, что backend доступен:**
   - Откройте ваш URL в браузере
   - Должна открыться документация Swagger

2. **Проверьте формат .env:**
   - Должно быть: `REACT_APP_API_URL=https://url.com`
   - БЕЗ пробелов вокруг `=`

3. **Очистите кэш:**
   ```bash
   rm -rf node_modules/.cache
   npm start
   ```

4. **Проверьте URL:**
   - Для Telegram WebApp должен быть HTTPS
   - URL должен быть доступен из интернета

Подробнее: `FULL_DIAGNOSTIC.md`

