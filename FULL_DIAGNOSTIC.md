# 🔍 Полная диагностика подключения Frontend ↔ Backend

## Шаг 1: Проверка .env файла

### Расположение файла
Файл должен быть: `miniapp/react-app/.env` (в корне проекта React)

**Проверьте:**
```bash
cd miniapp/react-app
dir .env    # Windows
ls -la .env # Linux/Mac
```

### Формат файла
Файл должен содержать:
```
REACT_APP_API_URL=https://ваш-backend-url.com
```

**Важно:**
- ✅ Начинается с `REACT_APP_`
- ✅ Нет пробелов: `REACT_APP_API_URL=https://url.com`
- ✅ Нет кавычек (если не нужны)
- ✅ URL начинается с `http://` или `https://`

### Автоматическая проверка
```bash
cd miniapp/react-app
node diagnose_connection.js
```

## Шаг 2: Проверка кода

### api.js использует переменную окружения
Файл `miniapp/react-app/src/services/api.js` должен содержать:
```javascript
const API_BASE_URL = process.env.REACT_APP_API_URL || 'http://localhost:8000';
```

✅ Код правильный - использует `process.env.REACT_APP_API_URL`

### Все функции используют API_BASE_URL
✅ Все функции используют `API_BASE_URL` из api.js

## Шаг 3: Проверка сервера

### Backend запущен?
```bash
cd backend
python run.py
```

Должно быть: `Uvicorn running on http://0.0.0.0:8000`

### Сервер доступен?
Откройте в браузере: http://localhost:8000/docs

Должна открыться документация Swagger.

### Тест подключения
```bash
cd miniapp/react-app
node test_api_connection.js https://ваш-backend-url.com
```

## Шаг 4: Проверка в браузере

### Откройте консоль (F12)

Должны быть сообщения:
```
API Base URL configured: https://ваш-url.com
REACT_APP_API_URL env: https://ваш-url.com
Checking API health at: https://ваш-url.com
```

### Если видите `undefined`:
- Файл .env не загружен
- Перезапустите приложение
- Проверьте формат файла

### Если видите `http://localhost:8000`:
- .env не загружен или неправильный
- Используется значение по умолчанию

## Шаг 5: Для Telegram WebApp

### Требования:
1. ✅ URL должен быть HTTPS (не http://)
2. ✅ Backend должен быть доступен по этому URL
3. ✅ CORS настроен (уже настроен в backend)

### Проверка:
1. Откройте приложение в Telegram
2. Откройте DevTools (через меню Telegram)
3. Проверьте консоль на ошибки

## 🔧 Быстрое исправление

### 1. Создайте/обновите .env
```bash
cd miniapp/react-app
# Windows
create_env.bat

# Linux/Mac
./create_env.sh
```

Или вручную создайте `miniapp/react-app/.env`:
```
REACT_APP_API_URL=https://ваш-backend-url.com
```

### 2. Остановите React приложение
Нажмите `Ctrl+C` в терминале

### 3. Очистите кэш (опционально)
```bash
# Windows
rmdir /s /q node_modules\.cache

# Linux/Mac
rm -rf node_modules/.cache
```

### 4. Запустите снова
```bash
npm start
```

### 5. Проверьте консоль
Должно быть:
```
API Base URL configured: https://ваш-url.com
```

## ❌ Частые проблемы

### Проблема 1: .env не загружается
**Решение:**
- Файл должен быть в `miniapp/react-app/.env`
- Называться `.env` (с точкой)
- Перезапустить приложение

### Проблема 2: Используется localhost вместо вашего URL
**Решение:**
- Проверьте .env файл
- Убедитесь, что нет пробелов
- Перезапустите приложение

### Проблема 3: Сервер недоступен в Telegram
**Решение:**
- URL должен быть HTTPS
- Backend должен быть доступен по этому URL
- Используйте туннель (ngrok, localtunnel)

### Проблема 4: CORS ошибка
**Решение:**
- Backend уже настроен с `allow_origins=["*"]`
- Проверьте `backend/app/main.py`

## 📋 Чеклист

- [ ] Файл `.env` создан в `miniapp/react-app/`
- [ ] Формат правильный: `REACT_APP_API_URL=https://url.com`
- [ ] Нет пробелов вокруг `=`
- [ ] Приложение перезапущено после создания .env
- [ ] Backend запущен и доступен
- [ ] В консоли браузера виден правильный URL
- [ ] Для Telegram: URL начинается с `https://`

## 🆘 Если ничего не помогает

1. Запустите диагностику:
   ```bash
   cd miniapp/react-app
   node diagnose_connection.js
   ```

2. Проверьте подключение:
   ```bash
   node test_api_connection.js https://ваш-url.com
   ```

3. Проверьте консоль браузера на ошибки

4. Убедитесь, что backend доступен по указанному URL

