# 🚀 БЫСТРЫЙ ПЕРЕЗАПУСК

## Проблема: React не видит .env файл

## ✅ РЕШЕНИЕ:

### Вариант 1: Используйте скрипт (рекомендуется)

**Windows:**
```bash
cd miniapp/react-app
.\restart.bat
```

**Linux/Mac:**
```bash
cd miniapp/react-app
chmod +x restart.sh
./restart.sh
```

### Вариант 2: Вручную

1. **Остановите React** (Ctrl+C)

2. **Очистите кэш:**
   ```bash
   cd miniapp/react-app
   # Windows PowerShell:
   Remove-Item -Recurse -Force node_modules\.cache
   
   # Linux/Mac:
   rm -rf node_modules/.cache
   ```

3. **Проверьте .env:**
   ```bash
   # Должен быть файл miniapp/react-app/.env
   # С содержимым:
   # REACT_APP_API_URL=https://cold-rice-yawn.loca.lt
   ```

4. **Запустите снова:**
   ```bash
   npm start
   ```

5. **Проверьте консоль браузера:**
   - Откройте DevTools (F12)
   - Вкладка Console
   - Должны увидеть:
     ```
     API Base URL configured: https://cold-rice-yawn.loca.lt
     REACT_APP_API_URL env: https://cold-rice-yawn.loca.lt
     ```

## ⚠️ ВАЖНО:

- **После изменения .env ОБЯЗАТЕЛЬНО перезапустите React**
- **Очистка кэша помогает в 90% случаев**
- **Проверяйте консоль браузера - там видно какой URL используется**

## 🔍 Если все еще не работает:

1. Проверьте что backend запущен: `cd backend && python run.py`
2. Проверьте что localtunnel запущен: `lt --port 8000`
3. Проверьте URL в браузере: https://cold-rice-yawn.loca.lt/docs
4. Проверьте консоль браузера на ошибки

