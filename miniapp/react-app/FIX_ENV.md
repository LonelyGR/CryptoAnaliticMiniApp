# Исправление проблемы с .env файлом

## Проблема: React не читает .env файл

### Причины и решения:

## ✅ Решение 1: Перезапустите приложение

**ВАЖНО:** После создания или изменения `.env` файла нужно **ПЕРЕЗАПУСТИТЬ** React приложение!

1. Остановите приложение (Ctrl+C в терминале)
2. Запустите снова:
   ```bash
   npm start
   ```

## ✅ Решение 2: Проверьте формат .env файла

Файл должен быть в папке `miniapp/react-app/.env` (не в src!)

**Правильный формат:**
```
REACT_APP_API_URL=https://your-url.com
```

**Неправильно:**
```
REACT_APP_API_URL = https://your-url.com  # Пробелы вокруг =
REACT_APP_API_URL=https://your-url.com   # Пробелы в конце
react_app_api_url=https://your-url.com    # Не REACT_APP_
```

## ✅ Решение 3: Проверьте имя файла

Файл должен называться **`.env`** (с точкой в начале), а не:
- ❌ `env`
- ❌ `.env.txt`
- ❌ `env.txt`

## ✅ Решение 4: Проверьте расположение файла

Файл должен быть в корне проекта React:
```
miniapp/react-app/
  ├── .env          ← ЗДЕСЬ!
  ├── package.json
  ├── src/
  └── ...
```

## ✅ Решение 5: Проверьте в консоли браузера

Откройте консоль браузера (F12) и найдите:
```
API Base URL configured: ...
REACT_APP_API_URL env: ...
```

Если видите `REACT_APP_API_URL env: undefined` - файл не загружен.

## 🔍 Проверка

Запустите скрипт проверки:
```bash
cd miniapp/react-app
node check_env.js
```

## 📝 Пример правильного .env файла

Создайте файл `miniapp/react-app/.env`:

```
REACT_APP_API_URL=https://abc123.ngrok.io
```

Или для localhost:
```
REACT_APP_API_URL=http://localhost:8000
```

## ⚠️ Важные правила

1. **Переменные должны начинаться с `REACT_APP_`**
2. **Нет пробелов вокруг `=`**
3. **Нет кавычек вокруг значения** (если не нужны)
4. **Перезапуск обязателен** после изменения

## 🚀 Быстрое исправление

1. Создайте файл `miniapp/react-app/.env`:
   ```
   REACT_APP_API_URL=https://your-backend-url.com
   ```

2. Остановите React приложение (Ctrl+C)

3. Запустите снова:
   ```bash
   npm start
   ```

4. Проверьте консоль браузера - должно быть:
   ```
   API Base URL configured: https://your-backend-url.com
   ```

## Если все еще не работает

1. Убедитесь, что файл называется `.env` (не `.env.local` или другое)
2. Проверьте, что нет пробелов: `REACT_APP_API_URL=https://url.com`
3. Убедитесь, что URL правильный (начинается с http:// или https://)
4. Перезапустите приложение
5. Очистите кэш: удалите папку `node_modules/.cache` и перезапустите

