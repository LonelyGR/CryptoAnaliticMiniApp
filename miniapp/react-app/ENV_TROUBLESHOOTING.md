# Решение проблемы с .env файлом

## 🔍 Диагностика

### Проверьте консоль браузера

Откройте DevTools (F12) → Console и найдите:
```
API Base URL configured: ...
REACT_APP_API_URL env: ...
```

**Если видите:**
- `REACT_APP_API_URL env: undefined` → файл не загружен
- `API Base URL configured: http://localhost:8000` → используется значение по умолчанию

## ✅ Решение

### 1. Создайте правильный .env файл

**Расположение:** `miniapp/react-app/.env` (в корне проекта, не в src!)

**Содержимое:**
```
REACT_APP_API_URL=https://ваш-backend-url.com
```

**Важно:**
- ✅ Начинается с `REACT_APP_`
- ✅ Нет пробелов вокруг `=`
- ✅ Нет кавычек (если не нужны)
- ✅ Файл называется `.env` (с точкой)

### 2. Перезапустите приложение

**ОБЯЗАТЕЛЬНО!** React загружает .env только при старте.

```bash
# Остановите (Ctrl+C)
# Запустите снова
npm start
```

### 3. Проверьте результат

В консоли должно быть:
```
API Base URL configured: https://ваш-backend-url.com
REACT_APP_API_URL env: https://ваш-backend-url.com
```

## 🛠️ Автоматическое создание

**Windows:**
```bash
cd miniapp/react-app
create_env.bat
```

**Linux/Mac:**
```bash
cd miniapp/react-app
chmod +x create_env.sh
./create_env.sh
```

## ❌ Частые ошибки

### Ошибка 1: Файл в неправильном месте
```
❌ miniapp/react-app/src/.env
✅ miniapp/react-app/.env
```

### Ошибка 2: Неправильное имя
```
❌ env
❌ .env.txt
✅ .env
```

### Ошибка 3: Неправильный формат
```
❌ REACT_APP_API_URL = https://url.com  (пробелы)
❌ react_app_api_url=https://url.com    (не REACT_APP_)
✅ REACT_APP_API_URL=https://url.com
```

### Ошибка 4: Не перезапустили приложение
```
❌ Изменили .env, но не перезапустили
✅ Изменили .env → Остановили → Запустили снова
```

## 🔄 Полная перезагрузка

Если ничего не помогает:

```bash
# 1. Остановите приложение
# 2. Удалите кэш
rm -rf node_modules/.cache
# Windows: rmdir /s /q node_modules\.cache

# 3. Запустите снова
npm start
```

## 📝 Примеры правильных .env

### Для ngrok:
```
REACT_APP_API_URL=https://abc123.ngrok.io
```

### Для localtunnel:
```
REACT_APP_API_URL=https://random-name.loca.lt
```

### Для localhost:
```
REACT_APP_API_URL=http://localhost:8000
```

### Для production:
```
REACT_APP_API_URL=https://api.yourdomain.com
```

## ✅ Проверка работы

После настройки в консоли браузера должно быть:
```
API Base URL configured: https://ваш-url.com
Checking API health at: https://ваш-url.com
API health check result: true 200
```

Если видите эти сообщения - все работает! 🎉

