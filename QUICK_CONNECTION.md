# Быстрое подключение Frontend ↔ Backend

## ✅ Шаг 1: Запустите Backend

```bash
cd backend
python run.py
```

Должно появиться: `Uvicorn running on http://0.0.0.0:8000`

## ✅ Шаг 2: Настройте URL (опционально)

Создайте файл `miniapp/react-app/.env`:

```
REACT_APP_API_URL=http://localhost:8000
```

**Примечание:** Если не создадите `.env`, будет использоваться значение по умолчанию `http://localhost:8000`

## ✅ Шаг 3: Запустите Frontend

```bash
cd miniapp/react-app
npm start
```

## ✅ Шаг 4: Проверьте подключение

1. Откройте http://localhost:3000
2. Откройте консоль браузера (F12)
3. Должны быть сообщения:
   - `API Base URL configured: http://localhost:8000`
   - `API health check result: true 200`

## 🎉 Готово!

Frontend подключен к Backend через HTTP!

## Если не работает

1. **Проверьте, что backend запущен:**
   - Откройте http://localhost:8000/docs в браузере
   - Должна открыться документация Swagger

2. **Проверьте консоль браузера:**
   - Если видите ошибки подключения - backend не запущен
   - Если видите CORS ошибки - проверьте настройки CORS

3. **Перезапустите React приложение:**
   - Остановите (Ctrl+C)
   - Запустите снова: `npm start`

## Для Telegram WebApp

Если приложение запускается в Telegram, `localhost` не будет работать. Используйте:
- ngrok туннель
- Или IP адрес вашего компьютера

Подробнее: `CONNECTION_GUIDE.md`

