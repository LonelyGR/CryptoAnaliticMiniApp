# 🔧 Настройка ngrok для Telegram Mini App

## Проблемы, которые были исправлены:

1. ✅ **Логика записей на вебинар** - исправлена работа с `user.id` vs `telegram_id`
2. ✅ **Создание пользователя в БД** - улучшена обработка пользователя из Telegram WebApp
3. ✅ **CORS настройки** - проверены и улучшены для работы с ngrok
4. ✅ **Обработка ошибок** - добавлено подробное логирование для отладки

## 📋 Инструкция по настройке:

### 1. Запустите Backend с ngrok

```bash
# В терминале 1: Запустите backend
cd backend
python -m uvicorn app.main:app --reload --port 8000

# В терминале 2: Запустите ngrok для backend
ngrok http 8000
```

**Важно:** Скопируйте HTTPS URL из ngrok (например: `https://abc123.ngrok-free.app`)

### 2. Настройте .env файл для фронтенда

Создайте файл `miniapp/react-app/.env` (если его нет):

```env
REACT_APP_API_URL=https://ваш-ngrok-url.ngrok-free.app
```

**⚠️ ВАЖНО:**
- Замените `ваш-ngrok-url.ngrok-free.app` на ваш реальный ngrok URL
- Не используйте пробелы вокруг `=`
- URL должен начинаться с `https://`
- После изменения .env файла **перезапустите React приложение**

### 3. Запустите Frontend с ngrok

```bash
# В терминале 3: Запустите React приложение
cd miniapp/react-app
npm start

# В терминале 4: Запустите ngrok для frontend
ngrok http 3000
```

**Важно:** Скопируйте HTTPS URL из ngrok для frontend

### 4. Настройте Telegram Bot

В настройках бота в BotFather укажите:
- **Web App URL:** `https://ваш-frontend-ngrok-url.ngrok-free.app`

## 🔍 Проверка работы:

### Проверка Backend:
1. Откройте в браузере: `https://ваш-backend-ngrok-url.ngrok-free.app/docs`
2. Должна открыться документация FastAPI

### Проверка Frontend:
1. Откройте в браузере: `https://ваш-frontend-ngrok-url.ngrok-free.app`
2. Откройте консоль разработчика (F12)
3. Проверьте логи:
   - `API Base URL configured: https://...`
   - `REACT_APP_API_URL env: https://...`
   - `Telegram WebApp user: {...}` (если открыто в Telegram)
   - `Пользователь загружен из БД: {...}`

### Проверка в Telegram:
1. Откройте мини-приложение в Telegram
2. Откройте консоль разработчика (если возможно)
3. Проверьте, что пользователь создается в БД
4. Попробуйте записаться на вебинар

## 🐛 Решение проблем:

### Проблема: "API недоступен" на телефоне
**Решение:**
1. Проверьте, что backend ngrok запущен
2. Проверьте, что в `.env` файле правильный URL backend ngrok
3. Перезапустите React приложение после изменения .env
4. Проверьте консоль браузера на ошибки CORS

### Проблема: "Пользователь не найден в базе данных"
**Решение:**
1. Проверьте, что пользователь открывает приложение через Telegram (не через браузер)
2. Проверьте логи в консоли - должно быть сообщение о создании пользователя
3. Проверьте, что backend доступен и работает

### Проблема: "Не удалось записаться на вебинар"
**Решение:**
1. Проверьте, что пользователь создан в БД (должен быть в консоли)
2. Проверьте логи backend на ошибки
3. Проверьте, что `user_id` правильно передается в запросе

## 📝 Важные замечания:

1. **ngrok URL меняется** при каждом перезапуске (в бесплатной версии)
   - После каждого перезапуска ngrok обновите `.env` файл и перезапустите React

2. **С подпиской ngrok** можно использовать статический домен
   - Настройте статический домен в ngrok dashboard
   - Используйте его в `.env` файле

3. **При доступе с ноутбука** (не через Telegram):
   - Telegram WebApp не доступен
   - Пользователь не будет создан в БД
   - Это нормальное поведение - приложение работает только в Telegram

4. **Логи для отладки:**
   - Все важные действия логируются в консоль браузера
   - Проверяйте консоль при возникновении проблем

## ✅ Что было исправлено в коде:

1. **backend/app/routers/users.py:**
   - Endpoint `/users/telegram/{telegram_id}` теперь правильно принимает данные из body

2. **backend/app/schemas/user.py:**
   - Добавлена схема `UserUpdate` для обновления пользователя

3. **miniapp/react-app/src/screens/Bookings.js:**
   - Исправлена логика получения `telegram_id` из объекта user
   - Улучшена обработка ошибок

4. **miniapp/react-app/src/App.js:**
   - Улучшена обработка пользователя из Telegram WebApp
   - Добавлено подробное логирование

5. **miniapp/react-app/src/services/api.js:**
   - Улучшена обработка ошибок
   - Добавлено подробное логирование запросов

6. **backend/app/main.py:**
   - Улучшены комментарии в CORS настройках

