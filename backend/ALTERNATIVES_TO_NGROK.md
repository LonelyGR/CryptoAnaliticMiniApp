# Альтернативы ngrok для Backend

Если ngrok уже используется на фронтенде, вот альтернативы для backend:

## 🚀 Вариант 1: localtunnel (Рекомендуется - простой)

### Установка:
```bash
npm install -g localtunnel
```

### Запуск:
```bash
lt --port 8000
```

Вы получите URL типа: `https://random-name.loca.lt`

### Плюсы:
- ✅ Бесплатно
- ✅ Не требует регистрации
- ✅ Простая установка
- ✅ HTTPS по умолчанию

### Минусы:
- ⚠️ URL меняется каждый раз
- ⚠️ Может быть нестабильным

### Настройка React:
Обновите `miniapp/react-app/.env`:
```
REACT_APP_API_URL=https://random-name.loca.lt
```

---

## 🌐 Вариант 2: Cloudflare Tunnel (Бесплатно, стабильно)

### Установка:
1. Скачайте: https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/install-and-setup/installation/
2. Или через пакетный менеджер:
   - Windows: `winget install --id Cloudflare.cloudflared`
   - Mac: `brew install cloudflared`
   - Linux: скачайте с сайта

### Запуск:
```bash
cloudflared tunnel --url http://localhost:8000
```

Вы получите URL типа: `https://random-name.trycloudflare.com`

### Плюсы:
- ✅ Бесплатно
- ✅ Стабильный
- ✅ HTTPS
- ✅ Не требует регистрации для простого использования

### Минусы:
- ⚠️ URL меняется при перезапуске

---

## 🔧 Вариант 3: serveo (SSH туннель)

### Запуск:
```bash
ssh -R 80:localhost:8000 serveo.net
```

### Плюсы:
- ✅ Не требует установки (если есть SSH)
- ✅ Бесплатно
- ✅ Можно задать свой поддомен: `ssh -R yourname:80:localhost:8000 serveo.net`

### Минусы:
- ⚠️ Требует SSH клиент
- ⚠️ Может быть нестабильным

---

## 🎯 Вариант 4: Использовать два ngrok (один для фронта, другой для бэка)

Если у вас есть зарегистрированный аккаунт ngrok, можно использовать два туннеля:

### Запуск:
```bash
# Терминал 1: Backend
ngrok http 8000

# Терминал 2: Frontend (если нужно)
ngrok http 3000
```

### Плюсы:
- ✅ Один инструмент для всего
- ✅ Стабильный
- ✅ Можно зафиксировать домен (платная версия)

---

## 🏗️ Вариант 5: Деплой на бесплатный хостинг

### Railway (Рекомендуется)
1. Зарегистрируйтесь: https://railway.app
2. Подключите GitHub репозиторий
3. Railway автоматически задеплоит и даст HTTPS URL

### Render
1. Зарегистрируйтесь: https://render.com
2. Создайте новый Web Service
3. Подключите репозиторий
4. Получите постоянный HTTPS URL

### Fly.io
1. Установите: `curl -L https://fly.io/install.sh | sh`
2. Зарегистрируйтесь: `fly auth signup`
3. Задеплойте: `fly launch`

---

## 📝 Быстрые команды

### localtunnel (самый простой):
```bash
npm install -g localtunnel
lt --port 8000 --print-requests
```

### Cloudflare Tunnel:
```bash
cloudflared tunnel --url http://localhost:8000
```

### serveo (с кастомным именем):
```bash
ssh -R your-backend:80:localhost:8000 serveo.net
```

---

## 🎯 Рекомендация

Для быстрого старта используйте **localtunnel** - самый простой вариант:
1. Установка: `npm install -g localtunnel`
2. Запуск: `lt --port 8000`
3. Скопируйте URL и обновите `.env`

Для production используйте **Railway** или **Render** - бесплатный хостинг с постоянным URL.

